import React from 'react';

export default function Notes(props) {
	
	return (
			<div className="notes">
				<dt>The card database is stored locally to cut down on request load to NRDB during testing.</dt>
			</div>
		);
}
